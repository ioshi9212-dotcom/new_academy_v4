# Redirect: roommate / NPC persistence

Main rules moved to:

```txt
gpt/locks/character_presence_rotation_lock.md
```

Use that file for roommates, recurring NPCs, dorm conflicts, known/unknown names, and persistence in state.

This file remains only for backward compatibility.
