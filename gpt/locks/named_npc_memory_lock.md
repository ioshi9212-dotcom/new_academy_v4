# Redirect: память об именованных NPC

Правила этого lock-файла объединены в:

```txt
gpt/locks/character_presence_rotation_lock.md
```

Использовать его как основной источник для:

- именованных NPC;
- повторяющихся NPC;
- NPC с конфликтом, ролью или шансом вернуться;
- сохранения NPC в `characters/npc`, `character_id_index`, `knowledge_state`, `relationships`, `scene_history` и `story_lines`;
- запрета терять NPC после сцены.

Этот файл оставлен только для обратной совместимости старых ссылок.
